machine:
  node:
    version: 0.12

dependencies:
  pre:
    - touch sites.yml

test:
  override:
    - echo "No tests written yet."

deployment:
  capistrano:
    branch: capistrano
    commands:
      - cap staging deploy
  staging:
    branch: master
    commands:
      - cap staging deploy
  production:
    branch: live
    commands:
      - cap production deploy
