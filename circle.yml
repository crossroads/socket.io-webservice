machine:
  node:
    version: 0.12

dependencies:
  pre:
    - touch sites.yml

test:
  override:
    - echo "No tests written yet."

deployment:
  staging:
    branch: master
    commands:
      - bundle exec cap staging deploy
  production:
    branch: live
    commands:
      - bundle exec cap production deploy
